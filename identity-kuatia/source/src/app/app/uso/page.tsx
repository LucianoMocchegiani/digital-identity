/**
 * Ruta `/app/uso` — cuota mensual, rate limit y límites del plan.
 */
import { UsagePanel } from '@/modules/account/components/UsagePanel'

export default function UsoPage() {
  return <UsagePanel />
}
