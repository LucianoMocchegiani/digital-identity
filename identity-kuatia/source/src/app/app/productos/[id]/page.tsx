/**
 * Ruta `/app/productos/[id]` — detalle, edición y gestión de API keys.
 */
import { ProductDetail } from '@/modules/products/components/ProductDetail'

export default function ProductoDetallePage() {
  return <ProductDetail />
}
