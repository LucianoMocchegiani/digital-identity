/**
 * Ruta `/app/productos` — listado de productos issuer/verifier de la cuenta.
 */
import { ProductList } from '@/modules/products/components/ProductList'

export default function ProductosPage() {
  return <ProductList />
}
