/**
 * Ruta `/app/cuenta` — perfil de la cuenta y cierre de sesión.
 */
import { AccountPanel } from '@/modules/account/components/AccountPanel'

export default function CuentaPage() {
  return <AccountPanel />
}
