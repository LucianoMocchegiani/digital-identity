/// Identificador lógico del wallet en disco.
///
/// Se usa como nombre del archivo Isar cifrado (`default.isar`) gestionado por
/// `identity_core_dart` y del JSON de preferencias locales (`default_ux.json`).
/// Debe coincidir en [WalletService], [WalletUxPaths] y [WalletNotifier].
const kWalletId = 'default';
